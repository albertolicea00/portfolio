[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  DEPLOY_TARGET = "netlify"
  SECRETS_SCAN_OMIT_KEYS = "TURNSTILE_SITE_ID"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
